CREATE TABLE KIND (
	ID VARCHAR(100),
	STAND TIMESTAMP,
	KIND_AKTEN VARCHAR(2000)
);

CREATE TABLE KINDMAPPE_ID (
	ID BIGINT
);

CREATE OR REPLACE VIEW KIND_AKTUELL AS
	SELECT DISTINCT ON (ID) *
	FROM KIND
	ORDER BY ID, STAND DESC;